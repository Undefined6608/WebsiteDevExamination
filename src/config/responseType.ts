// logo的类型
export type GetLogoType = {
    icon: string
}
// 副标题的类型
export type GetSubTitle = {
    title: string
}
// 导航列表内部对象类型
export type GetNavItem = {
    id: string,
    name: string,
    subName: string,
    url: string
}